CREATE TABLE IF NOT EXISTS setting (name VARCHAR(50) NOT NULL, type VARCHAR(50) NOT NULL, value VARCHAR(50) NOT NULL, description VARCHAR(255) NOT NULL, module VARCHAR(50) NOT NULL, verified TINYINT NOT NULL);